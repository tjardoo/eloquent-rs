[package]
name = "eloquent-demo"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]
members = ["eloquent", "eloquent_core"]

[dependencies]
eloquent = { path = "eloquent", version = "2.0" }
